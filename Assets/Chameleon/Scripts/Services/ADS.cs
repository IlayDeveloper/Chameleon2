using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
//using EasyMobile;
/* 
public class ADS : MonoBehaviour {

	private bool isReadyReward = false;
	public Text interStatus;
	public Text rewardStatus;
	public Text emobileStatus;

	void Awake()
	{
		if (!RuntimeManager.IsInitialized())
		RuntimeManager.Init();
	}

	void Update ()
	{
		/*if (Advertising.IsRewardedAdReady())
		{
			rewardStatus.text = "Reward is ready!";
		}
		else
		{
			rewardStatus.text = "...Reward is not ready...";
		}

		if(Advertising.IsInterstitialAdReady())
		{
			interStatus.text = "Inerstitial is ready!";
		}
		else
		{
			interStatus.text = "...Inerstitial is not ready...";
		}

		if (RuntimeManager.IsInitialized())
		{
			emobileStatus.text = "emobile is initilized!";
		}
		else
		{
			emobileStatus.text = "...emobile not init...";
		}
	}
	void Start () 
	{
		//Advertising.GrantDataPrivacyConsent();
		//Advertising.LoadRewardedAd();
		//Advertising.LoadInterstitialAd();
	}
	

	public void ShowReward ()
	{
		if (Advertising.IsRewardedAdReady())
		{
			Advertising.ShowRewardedAd();
		}
		else
		{
			Advertising.LoadRewardedAd();
		}
	}

	public void ShowInter ()
	{
		if (Advertising.IsInterstitialAdReady())
		{
			Advertising.ShowInterstitialAd();
		}
		else
		{
			Advertising.LoadInterstitialAd();
		}
	}

	public void ShowBanner ()
	{
		Advertising.ShowBannerAd(BannerAdPosition.Bottom);
	}

}
	*/
